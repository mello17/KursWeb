@model IQueryable<DAL.Web.Site.Models.News>
@{
    ViewBag.Title = "StudentsView";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<body>
    <h2>Not Students. GraduateView</h2>
   @* @Html.Action("_PartialIndexArticles", Model)*@
   @* @Html.Action("_PartialLayoutNews")*@
    @*  Достало. У меня нет миграций, я не имею доступа к БД и все равно не могу извлечь данные. 

        <table class="table">
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Class.letter)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.FirstName)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.LastName)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.FathersName)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Email)
                </th>
                <th></th>
            </tr>

            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Class.letter)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.FirstName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.LastName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.FathersName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Email)
                    </td>
                    <td>
                        @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                          @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                          @Html.ActionLink("Delete", "Delete", new { id = item.Id })*@
    </td>
    </tr>
    }

    </table>
    </body>
