@model IEnumerable<DAL.Models.Graduate>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Group.Group_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ScienceWork.Theme)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Teacher.FIO)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FIO)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Group.Group_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ScienceWork.Theme)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Teacher.FIO)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FIO)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
