@model IEnumerable<DAL.Models.Schedule>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Course.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Group.Group_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Teacher.FIO)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Time)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Auditory)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Course.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Group.Group_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Teacher.FIO)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Time)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Auditory)
        </td>
        <td>
            
            <a href="/Schedules/Edit?id=@item.Id">Edit</a>  |
            <a href="/Schedules/Delete?id=@item.Id">Edit</a>  
        </td>
    </tr>
}

</table>
